A prototype tool that enables one to create a form via a browser. 

Currently it lets you add text fields, text areas, selects, checkboxes and radio buttons. It also lets you sort and delete fields.

Still todo: Edit labels, options and help text for individual fields.